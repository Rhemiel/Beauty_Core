/** @type {import('next').NextConfig} */
const nextConfig = {
  // Turbopack is usually disabled by default in Next.js 14/15 unless --turbo is passed.
  // We'll keep this minimal.
};

export default nextConfig;
